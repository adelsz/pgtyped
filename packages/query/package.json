{
  "name": "@pg-typed/query",
  "version": "0.0.1",
  "main": "lib/index.js",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "build": "tsc --declaration",
    "check": "tsc --noEmit",
    "watch": "yarn build --watch --preserveWatchOutput"
  },
  "jest": {
    "roots": [
      "src"
    ],
    "transform": {
      "^.+\\.tsx?$": "ts-jest"
    }
  },
  "dependencies": {
    "@pg-typed/wire": "0.0.1",
    "@types/debug": "^4.1.4",
    "debug": "^4.1.1"
  }
}
