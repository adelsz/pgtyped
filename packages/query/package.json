{
  "name": "@pgtyped/query",
  "version": "0.1.0",
  "main": "lib/index.js",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "test": "jest",
    "build": "tsc --declaration",
    "check": "tsc --noEmit",
    "watch": "tsc --declaration --watch --preserveWatchOutput"
  },
  "jest": {
    "roots": [
      "src"
    ],
    "transform": {
      "^.+\\.tsx?$": "ts-jest"
    }
  },
  "dependencies": {
    "@pgtyped/wire": "^0.1.0",
    "@types/debug": "^4.1.4",
    "debug": "^4.1.1"
  }
}
